import * as React from 'react';

import Navigation from '../Navigation';

const Header = () => (
  <div className="header">
    <span className="brand">JAR?!</span>
    <Navigation />
  </div>
);

export default Header;
